#include <stdio.h>
int main(int argc, char *argv[])
{
	printf("argc %d\n", argc);
	printf("argv[0] = %s\n", argv[0]);
	printf("argv[1] = %s\n", argv[1]);
	printf("argv[2] = %s\n", argv[2]);

	/*fd_r = fopen(argv[1], "r");
	fd_w = fopen(argv[2], "w");

	while(EOF)
	{
		read(fd_r, ibuf, 1);
		if(ibuf[0] = 'CR'
		{
			continue;			
		}
		else
			write(fd_w)*/
	return 0;
}		 
